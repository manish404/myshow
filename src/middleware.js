export function middleware(req) { };
/* import { NextResponse } from "next/server";
import println from "./helpers/print";

const PORT = process.env.PORT || 3000;

const paths = ['/admin', '/profile', '/api', '/booking', '/movies', '/halls'];
// const pattern = new RegExp(`${PORT}(${paths.join('|')}).*`);
// println('MIDDLEWARE :', pattern);
// // const patternAsString = pattern.toString();
const name = 'sb-vrhyeprulbnxbynjopmp';

export async function middleware(req) {
    const res = NextResponse.next();
    // const { href } = req.nextUrl;
    // if (pattern.test(href)) {
    // println('Hits middleware > ', href);
    // const [token] = req.cookies.get(`${name}-auth-token`).value;
    // println(token ? 'authenticated' : 'unauthenticated', '!');


    //         // NextResponse.redirect('/');
    // }
    return res;
} */